#include <iostream>
using namespace std;
int main()
class users
{ public:
int age;
string name;
float height;
float weight;

void getuserinfo(int n,string m, float z)
{
	n=age;
	m=name;
	z=height;
}
double calculateBMI(double weight, double height) {
    // BMI formula: weight (kg) / (height (m) * height (m))
    return weight / (height * height);
}
};
{
	return 0;
}
